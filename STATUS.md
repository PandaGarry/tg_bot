# Состояние работы

Что уже реализовано, чем проверено и что открыто. Отметки в [06-plan.md](docs/game/06-plan.md) ставит заказчик:
шаги 1–3 отмечены готовыми 25.09.2026, дальше — шаг 4 «Двор».

Ветка: `arena/01a0d5c4-tg-bot`. Коммиты — по шагам, сообщения по-русски.

**Вид игры (25.09.2026).** Три первых концепта отменены и удалены: строились от чёрного наброска заглушки,
а не от разбора жанра (официальные экраны лидеров — яркие, мультяшные, никакой мрачности). Принят процесс
заказчика: **разбор → скелет → до 10 вариантов → выбор**; список 12 игр и ширина «всё визуальное» утверждены.

Сделано:
- **Разбор, партия 1 — 4 игры по настоящим скриншотам**: Whiteout Survival, Last War, Kingshot, Rise of Kingdoms.
  Сводка общего языка жанра («шапка панели тёмная, главная кнопка справа в шапке»; «голубые угловые скобки»;
  «зелёный плюс = можно улучшить»; «зелёный — язык выгоды, а не украшение»; «бейджи внимания, 3–5»;
  «социальное видно на сцене»; «числа сокращённые, точное — в один тап») и «чего не берём» (семь ресурсов
  в строке, смесь форматов чисел, рекламные плашки поверх игры) — [17-ui.md](docs/game/17-ui.md) §13.
- **Макет — живой скелет в коде**: `docs/game/ui/mockup.html`. Верх (лорд с уровнем, часы, зелёный чип выгоды,
  пять ресурсов), сцена, бейджи над объектами, боковая колонка кнопок, нижняя навигация из пяти пунктов,
  панель постройки снизу (тёмная шапка, «Улучшить» справа, чипы характеристик, сетка 5×3 с уровнями и плюсами,
  угловые скобки на выбранной, две вкладки), свайп закрывает панель. Палитра — переменные: стиль меняется
  без переделки раскладки. Как открыть: `cd docs/game/ui && python3 -m http.server 8080 --bind 0.0.0.0`.
- **Тестовые генерации** — четыре настроения одного сюжета (`docs/game/ui/scene-*.jpg`): яркое дневное,
  холодное сумеречное, крафтовое земляное, светящееся ночное. Переключаются в макете кнопками 1–4.

3D: проверено, что в песочнице доступны `three`, `@react-three/fiber`, `drei`, `postprocessing`,
`@gltf-transform/cli`, `@pmndrs/assets`; 3D-редактора нет — значит модели либо процедурные (кодом), либо
покупные. Рекомендация: процедурный стилизованный 3D-город + плоский 2D-интерфейс (у лидеров так же).
Про браузер: Chromium в песочнице **ставится и запускается** (пакет `@sparticuz/chromium` + библиотеки
`al2023` + SwiftShader, версия 153), но **рендеринг страницы виснет** — кадры смотрит заказчик через превью.
Заведён **шаг 3.5 «Лист вида и набор интерфейса»**; экраны шагов 4–9 собираются из набора.

**Бюджет двора решён:** верхние уровни не срезаем — двор остаётся 40 млн дерева / 14 суток на 25-м уровне,
доход переводится на карту (бюджет сезона пересчитан: дерево ≈120 млн, ~45% с карты) —
[09-numbers.md](docs/game/09-numbers.md) §0.

**Направление (уточнено заказчиком 25.09.2026):** долгая игра на развитие — уровень построек, сила командиров,
количество армии. **Прогресс не отнимается никогда**: без сбросов и обнулений, потолок только поднимается.
Цель — прибыль заказчику; комфорт, разнообразие и свобода — игрокам. Полная картина: [16-money.md](docs/game/16-money.md)
(семь слоёв покупок, показатели с целями, догоняющая лестница без сбросов, привлечение без магазинов).

| Шаг | Код | Тесты | Просмотр заказчиком |
|---|---|---|---|
| 1. Ядро | **готов** | ядро 44, хост 89, сервер 34 + приёмка на живом мире | принят 25.09.2026 |
| 2. Ворота | **готов** | 13 тестов ворот (хост), 15 тестов контура (сервер) | принят 25.09.2026 |
| 3. Оболочка и лорд | **готов** | 15 тестов оболочки (клиент) | принят 25.09.2026 |

Всего 183 теста, `pnpm typecheck`, `pnpm test`, `pnpm check`, `pnpm build` зелёные.

## Решения и открытые вопросы

Правило: решение заказчика записывается в [06-plan.md](docs/game/06-plan.md) в тот же заход, когда принято.
Принято 25.09.2026: общий аккаунт и своя база у каждого мира; вход по подписанному токену; одно время на всех
(события и сезонность централизованы); обновления через параллельный мир и рестарт; контент сначала кодом
с датой вступления; автоподключение к свежему миру, выбор — в настройках; служебный мир игрокам не виден;
принцип фаз мира; один лорд на мир; предел мира 5000 (цель 3000) и перелив в предыдущий мир; открытие
новых миров 1-го и 15-го числа. Подробности — [12-worlds.md](docs/game/12-worlds.md).

Проведён плотный анализ похожих игр — [13-genre-servers.md](docs/game/13-genre-servers.md): сроки открытия
серверов (RoK ~30 ч, Whiteout 1–2 дня, Evony ~36 ч, Last War 2–3 дня), вместимость (1–3,5 тыс.), фазы по возрасту
мира (столица ~день 53–60, межсерверные бои ~день 80–90), слияния (Evony) и переносы (Whiteout, Last War, RoK),
поддержание паритета между мирами.

Итог анализа: **награды считаются по возрасту мира** — `база полосы × результат × ассортимент возраста`
(прежний вывод «награда одна на всех» отменён заказчиком 25.09.2026); догоняет новичка свежий старт, полосы
возрастов и правила переноса. Закрыто 25.09.2026: полосы 0–15 / 15–45 / 45–90 / 90+;
новичковый срок лорда 7 суток ×1,5 к опыту и производству; открытие миров 1-го и 15-го числа; предел мира 5000
(цель 3000); один лорд на мир; 5 миров на узел базы с допуском по нагрузке; журнал 30 суток горячим;
служебный мир — отдельный сервер, виден только администрации.

Открытых вопросов по теме миров не осталось: **сезон — 3 месяца, мир живёт до слияния** (первое слияние
примерно через 4 месяца, далее каждые 3–6 месяцев по порогу активности); фазы 0–15 / 15–45 / 45–90; открытия
1-го и 15-го числа. Миры в строю не закрываются по сроку, а сливаются — поэтому число миров накапливается.
Параллельный мир — просто отдельный сервер, виден только администрации.

---

## Глубокий разбор 14 игр (25.09.2026)

Второй, более широкий прогон по требованию заказчика: минимум 10 игр — разобрано **14**
([15-genre-deep.md](docs/game/15-genre-deep.md)): RoK, Whiteout, Evony, Last War, GoT: Conquest, Viking Rise,
Lords Mobile, State of Survival, King of Avalon, Clash of Kings, Call of Dragons, Kingshot, Guns of Glory,
Vikings War of Clans.

Главные результаты:
- **Мир по календарю не закрывают нигде.** 3 месяца (GoT), 50 дней (Call of Dragons), 50–60 дней (RoK) —
  это сезон. Мир живёт, пока есть игроки, а пустеет — его сливают: у GoT первое слияние **~через 4 месяца**,
  дальше **каждые 3–6 месяцев** по здоровью баланса; пары — равного развития (Evony, Whiteout), уведомление
  за неделю, прогресс сохраняется.
- **Война миров начинается на 60–90 день** (Evony 60, Whiteout 80, RoK 85–95, Kingshot 76–103). Kingshot
  вручную замораживает таймлайн младших королевств на 2 недели, чтобы группы совпали по возрасту.
- **Награды за события отличаются** — и считаются тремя факторами: база полосы × результат (победа ×2,
  поражение ×0,5 — Whiteout, Evony) × ассортимент возраста (Whiteout: «Items change/Increase according to
  the state age»; RoK: «Event rewards may vary depending on your kingdom's season»), плюс сезонная валюта.
- **База расчётов по зданиям:** 10 игр с числами (RoK 25 ур. — 82,25 млн и 126 суток; Last War 35 ур. —
  2,1 млрд и 149 суток; Whiteout 30 ур. — 300 млн и 40 суток; Clash of Kings 30 ур. — 231 млн и 23,8 суток;
  State of Survival 30 ур. — 290 млн и 39,5 суток; Lords Mobile замок 24→25 — 139 суток без бонусов).
  Общие правила: старт — секунды, середина — сутки, верх — недели; новый ресурс на верхней трети
  (нефть, мифрил, Blackstone, плазма, Fire Crystal).

Внесено в решения: сезон 3 месяца, мир до слияния; полосы 0–15 / 15–45 / 45–90 / 90+; формула награды;
порог войны 45–60 день.

## Глубокий разбор жанра (25.09.2026)

По просьбе заказчика — «глубочайший анализ GoT, RoK, Viking Rise и подобных» — сделан разбор расчётов,
поселения, персонажа и сезонов: [14-genre-core.md](docs/game/14-genre-core.md). Собраны точные числа
из открытых таблиц игр: ратуша RoK 1→25 (1 800 ресурсов и 1 секунда → 82,25 млн и 126 суток), штаб
Last War 1→35 (14,26 млрд еды, 1056 суток базового времени), кривая исследований Viking Rise (7% → 18%
скорости, последний уровень 38 суток), герой RoK (60 уровень, 74 таланта, ~710 скульптур, 50,7 млн опыта),
генерал Evony (уровни 25/30/35/40 — 26 млн / 109 млн / 388 млн / 1,14 млрд опыта), дом GoT (50 уровней,
7 кормлений дракона в сутки, 25 000 Dragon Lore за 1→2), сезон GoT (3 месяца + месяц миграции, сезонная
валюта не сгорает), KvK RoK (старт на 85–95 день, 60 дней), Viking Rise (святыня ~50 день, миграция ~60).

Выведено и внесено в план: три категории бонусов A/B/C; время только делением на (1 + сумма скорости);
кривые стоимости ×1,5–2 за уровень и времени ×1,4; ёмкость здания = 10 часов производства; удвоение
на последнем уровне; предпосылки апгрейдов; новый редкий ресурс на верхней трети; персонаж — 60 уровень,
ступени, навыки отдельным ресурсом, таланты только у первого командира; бой через
`атака / (защита × здоровье)`. Возраст мира меняет **набор** наград и открытый контент, а количество считает
формула награды ([12-worlds.md](docs/game/12-worlds.md) §0).

Числа подготовлены: [09-numbers.md](docs/game/09-numbers.md) — черновик по образцу жанра, масштабированный
под мир в 3 месяца (производство зданий, ёмкости, двор 1–25, паутина, бонусы A/B/C, лорд, войско, точки
карты). Ждёт утверждения; нерешённое помечено в разделе 10 документа.

Числа: бюджет двора **решён** (верхние уровни не срезаем, доход переводится на карту, бюджет сезона пересчитан —
дерево ≈120 млн, ~45% с карты). Остальные числа ждут утверждения заказчика; нерешённое — [09-numbers.md](docs/game/09-numbers.md) §10.

## Что дальше (порядок для следующего захода)

1. **Разбор вида, партия 2** — Evony, Game of Thrones: Conquest, State of Survival, Lords Mobile
   ([17-ui.md](docs/game/17-ui.md) §4 — список из 12 утверждён заказчиком).
2. **Разбор вида, партия 3** — Call of Dragons, Warpath, Age of Empires Mobile, Puzzles & Survival,
   King of Avalon (антипример).
3. **Сводка и карта уникальности** — «роль → как решают все → разброс», где тесно, где пусто.
4. **Скелет всех экранов** — двор, карта, марш, командир, бой, отчёт, союз, события, магазин, настройки
   (сейчас в макете только двор).
5. **3–5 вариантов стиля на скелете** — из них заказчик выбирает; только после выбора рисуется лист вида.

Параллельно и независимо: **шаг 4 «Двор» (модуль `court`)** — числа готовы, ждёт слова заказчика о старте.

Ждут решения заказчика: якорные элементы (после вариантов); прототип 3D-сцены (только когда выберем стиль);
перенос даты открытия на 1-е и 15-е уже принят.

---

## Шаг 1. Ядро

Игровых экранов ещё нет. Проверяется контракт, часы и один писатель.

### Пакеты и стек

`pnpm-workspace.yaml`: `apps/client`, `apps/server`, `packages/kernel`, `packages/protocol`, `packages/host`, `packages/modules`, плюс `tools/*`. Turborepo, TypeScript, Zod, Vitest, Drizzle, Postgres.

Размер (строк в `src`, без тестов): ядро 1874, протокол 507, хост 3919, модули 866, сервер 1076, клиент 1562, инструменты 1137.

Узел: цель — Node 24 LTS, в песочнице сборка идёт на 22.22.3 (бинарник 24 туда не поставить). Кода, зависящего от версии, нет; `engines` держит `>=22.12`.

### Один процесс

`apps/server/src/boot.ts`: миграции, простой, состояния модулей, право писателя, приём команд. `apps/server/src/http.ts` отдаёт статику клиента, `/api/*` и сокет на одном порту. `0.0.0.0`, порт из окружения.

Порядок подъёма: **порт занимается до права писателя**. Процесс, которому негде слушать, уходит и не отбирает мир у живого соседа (иначе живой мир замолкал — это был настоящий дефект, нашёлся на приёмке).

### Реестр модулей

`tools/gen-registry.mjs` сканирует `packages/modules` и пишет `registry.gen.ts`. Ломаный контракт роняет сборку. `pnpm check` проверяет три вещи: реестр в порядке, границы пакетов (ядро без имён игры, модуль не импортирует модуль), словари (ключ есть и в `ru`, и в `en`).

### Сроки, склад, право писателя

Таблицы: `worlds`, `world_pulse`, `writer_leases`, `deadlines`, `stock`, `gathers`, `reports`, `commands`, `accounts`, `sessions`, `lords`, `module_states`, `audit_log`, `sim_rejections`.

- Сроки в базе, `pumpOnce()` берёт только наступившее, пакет не держит цикл дольше 50 мс.
- Склад: любой объявленный ресурс, только целые числа, минус невозможен, предел обрезает.
- Право писателя: `worlds.epoch` + `writer_leases`. Старый процесс после нового не пишет и сам заканчивает работу.
- Простой: `worlds.clock_offset_ms`, пульс каждые 10 секунд. Убийство процесса и плановый рестарт оставляют срок на месте.
- Повтор команды и повтора срока не удваивает выдачу: ключи идемпотентности, ответ `repeat: true`.
- Журналы: `audit`, `sim`, `security`, `access`, `app`. Клиенту уходит ключ словаря, а не след исключения.

### `_probe`

Пробный модуль доказывает, что ядро не знает имён игры: свой ресурс `probe_dust`, бонус производства, своя таблица `probe_state`, свой срок такта. Проверено на живом `world-1`: `stock.probe_dust` 83, `modules._probe.level` 3.

### Приёмка на живом мире (25.09.2026, `world-1`)

| Что | Как | Итог |
|---|---|---|
| Ресурс в складе | вход лордом, `_probe.poke` на 3 шага | 83 пыли, уровень 3 |
| Жёсткое падение | `kill -9`, простой 41,7 с | ресурс на месте, остаток срока 272,4 с против 272,8 с |
| Плановый рестарт | SIGTERM, простой 5,8 с | пульс записан, прогресс не потерян |
| Второй процесс на том же мире | поднять рядом | первый замолкает, второй ведёт |
| Второй процесс на том же порту | поднять рядом | уходит, право писателя не тронуто |
| Напор | 150 сессий, 6000 команд | потерь 0, отказы только `busy` и `stale`, лаг сроков 0 |
| Медленный клиент | задержать чтение сокета | соединение закрыто кодом 1013, память не растёт |

Инструмент: `pnpm load -- --sessions N --commands M --rate R [--open-loop true]` — те же команды, что у клиента, в базу мимо писателя не пишет.

---

## Шаг 2. Ворота

`packages/host/src/gates/gates.ts`, тесты `packages/host/tests/gates.test.ts` (8).

- **Регистрация и сессия вне писателя мира.** Ворота не поднимают писателя и не пишут склад: закрытая регистрация не трогает марши.
- **Пароль — Argon2id**: 19 МиБ, 2 прохода, 1 поток. В базе хеш, не открытый текст.
- **Секрет только в окружении**: `SESSION_SECRET` из `.env`. Токен лежит хешем `sha256(secret:token)`, живёт 30 дней.
- **Подделанный и протухший токен** не пускают; вход по тому же паролю проходит, по другому — нет.
- **Версия протокола** проверяется и на входе, и при возобновлении сессии.
- **Лорд**: имя 2–18 знаков (буквы, цифры, пробел, дефис), уникально в мире без учёта регистра и пробелов; портрет, знак и цвет — из готового набора, разметка не проходит; повторное создание возвращает того же лорда, второго не заводит.

Регистрация аккаунта — **отдельное окно от создания персонажа**: логин, почта, пароль, повтор пароля, согласие с правилами, разрешение на письма, кнопка «Зарегистрироваться».
Логин — технический вход (латиница, цифры, точка, дефис, подчёркивание, 3–24 знака), никнейм — имя лорда, которое игрок выбирает позже. Вход принимает и логин, и почту.
Без согласия с правилами аккаунт не заводится; почта проверяется на вид (одна «собака», точка в домене) — полное подтверждение письмом будет вместе с почтовым модулем.

Провод: `apps/server/src/net.ts` (`auth.register`, `auth.login`, `auth.token`, `lord.create`), `apps/server/tests/socket.test.ts` (14 тестов контура).

---

## Шаг 3. Оболочка и лорд

`apps/client/src/ui/*`, тесты `apps/client/tests/shell.test.tsx` (7).

- **Вступление**: восемь кадров из [03-first-session.md](docs/game/03-first-session.md), текст крупный и короткий, картинки — простая графика на палитре двора. Пропуск со второго кадра. Крутится один раз на устройстве: отметка в хранилище браузера, а не на сервере. Последний кадр ведёт к созданию лорда.
- **Хроника**: тексты хранятся ключами словаря, поэтому смена языка переводит и прошлые записи. Слайды попадают туда целиком, даже если игрок пропустил кадры. Вкладка «Хроника» внизу экрана.
- **Создание лорда**: имя, восемь портретов, шесть знаков, восемь цветов, три карточки типов с одной строкой бонуса. Кнопка «Занять двор» закрыта, пока имя короткое.
- **Двор**: фраза хрониста про частокол сказана один раз и лежит в хронике.
- **Русский и английский** на первом экране сразу; `pnpm check` следит за паритетом ключей (оболочка: 65 ключей).
- **Экран целиком**: `viewport-fit=cover`, `user-scalable=no`, `touch-action: manipulation`, цель не меньше 44 px, вёрстка на `100dvh` и `max-w-3xl`.

### Что в шаге 3 не проверено без браузера

Живые ширины 390, 768, 1280 и «двойной тап не зумит страницу» — это смотрит человек на устройстве через предпросмотр Replit. Автотесты проверяют тексты, порядок экранов, вступление, хронику, форму лорда и запрет на лорда без имени.

Отдельно ждёт заказчика приёмка шага 3: «чужой человек на телефоне и на широком экране доходит до двора и видит его после обновления».

---

## После просмотра: модули и регистрация

### Выключение и включение модуля

- **Выключить модуль может только он сам.** Чужая команда выключения отвергается ключом `kernel.command.forbidden`, в отчёт симуляции идёт строка `module.foreign-switch`: событие одного модуля не гасит соседей.
- **Включение — путь оператора.** У выключенного модуля нет ни команд, ни сроков, поэтому сам себя он включить не может; ручка `POST /api/modules` с ключом `ADMIN_TOKEN` возвращает его в строй, просроченные сроки догоняют ход и проводятся по одному разу каждый.
- **Ручка закрыта по умолчанию**: пустой `ADMIN_TOKEN` отвечает 403; неверный ключ пишет `admin.denied` в журнал безопасности; незнакомый модуль — 404. Состояния видны в `GET /api/modules` и в `/api/health`.

Проверено живьём на `world-1`: без ключа 403, чужой ключ 403, `_probe` выключен и включён обратно, `court` → 404.
Экранная кнопка переключения появится вместе с админ-панелью (шаг 10 плана); до неё путь оператора — ручка, а не игра.

### Регистрация и персонаж — два окна

| Окно | Поля |
|---|---|
| Регистрация аккаунта | логин, почта, пароль, повтор пароля, согласие с правилами, разрешение на письма, кнопка |
| Создание персонажа | имя лорда, портрет, знак, цвет, тип |

- Логин и никнейм — разные понятия: логин технический и на экране персонажа не показывается.
- В базе появились `accounts.email`, `accounts.email_key`, `accounts.accept_mail` (миграция `0001_account_email`), вход принимает и логин, и почту.
- Отказы ядра: `kernel.account.email-bad`, `kernel.account.rules`, `kernel.login.failed` — игрок видит фразу словаря, а не след исключения.

### Найден и починен дефект разработки

- `tsx watch` и Vite гоняли друг друга: Vite писал рядом временный файл конфига, watch видел его и перезапускал мир каждые 1,3 секунды. Теперь конфиг клиента читает загрузчик Node (`configLoader: "runner"`), мир держит один подъём.
- Отчёт игроку в миг отказа склада оставлял необработанный отказ и мог уронить процесс. Теперь снимок вида — необязательная часть отчёта, отказ склада не роняет мир; на это есть тест (проверено: без правки тест краснеет).

## Календарь и точечные переключатели (добавка в шаг 1)

Устройство: [10-modules-kinds.md](docs/game/10-modules-kinds.md), числа и даты: [11-calendar.md](docs/game/11-calendar.md).

### Что построено

| Слой | Модуль | Единицы | Как включается |
|---|---|---|---|
| окно дня | `day-window` | `gather-day`, `build-day`, `train-day`, `march-day`, `wall-day` | каждый день, старт 12:00 часов мира, кулдаун 4 дня |
| событие недели | `week-window` | четыре недели | понедельник, шесть дней |
| большое событие | `month-window` | `capital-ten-day` | 1-е число, модуль рождён выключенным |
| праздники | `holiday` | Новый год, Валентин, 8 марта, Пасха, лето, годовщина, Хеллоуин | точные даты, просвет 7 дней |
| часовые окна | `hour-window` | стража, обоз, сокровищница | 18:00/20:00/22:00, по часу |
| поры года | `season-*` | пора + вид | стык дат, ровно одна включена |

### Как решается, что включено

Одно место — чистая функция `unitStates` в ядре. Порядок силы: точечный запрет оператора → запрет модуля → окно
расписания → вид модуля. Причины видны снаружи: `operator`, `quarantine`, `module-off`, `module-closed`, `window`,
`between-windows`, `always-on`, `core`.

### Срок возврата работает по-настоящему

Две ошибки нашлись на живой проверке и починены:

- **Действующее состояние модуля считалось по хранимой строке**, поэтому запрет с истёкшим сроком продолжал держать
  модуль закрытым до перезапуска мира. Теперь `moduleIsEnabled` считает срок, а писатель раз в 5 секунд снимает
  просроченные запреты: `module.service.restored` и `unit.restored` в журнале, строки в базе чистятся.
- **Пустые записи копились**: снятие запрета оставляло строку `enabled` со сроком 0. Теперь такая запись удаляется,
  в `unit_states` живут только действующие переключатели.

### Проверено живьём на `world-1`

| Что | Как | Итог |
|---|---|---|
| Список модулей и единиц | `GET /api/modules` без ключа | 403, в журнале `admin.denied` |
| Список с ключом | 10 модулей, 28 единиц, у каждой состояние и причина | `month-window` — `module-closed`, праздники — `between-windows`, пора — `window` |
| Точечно погасить сезон | `POST { id, unitId: "autumn", until, reason }` | `disabled`/`quarantine`, окно при этом видно панели |
| Пережить перезапуск | SIGTERM, подъём (epoch 2) | запрет на месте, `until` тот же |
| Снять запрет | `POST { state: "enabled" }` | состояние вернулось к расписанию (`window`) |
| Сервисное выключение модуля | `hour-window` с `until` на 8 с | через 16 с модуль сам в строю, единицы — `between-windows`, строки в базе пусты |
| Чужая единица | `POST { unitId: "нет-такой" }` | 404 |
| Расписание | `GET /api/schedule?days=7` | 19 окон: пора, неделя, пять окон дня, часовые |
| Мусорный параметр | `?days=абв` | не роняет ручку, берётся 30 дней |

Печать плана: `pnpm schedule --days 90` (окна по часам мира, состояния единиц на выбранный день).
Проверка календаря при сборке: `pnpm check` считает 2026, 2027 и 2028 годы — пробелы, наложения, покрытие пор года.

## Доработка модулей: карантин, сторож, догон, счётчики

Устройство: [10-modules-kinds.md](docs/game/10-modules-kinds.md), разделы 3–6; часы: [08-ops.md](docs/game/08-ops.md).

### Что построено

- **Карантин и сторож.** Три отказа подряд за 60 с или обработчик дольше 250 мс убирают единицу с расчёта: лечение
  с отступом 60 с → 5 мин → 30 мин, четвёртая неудача требует оператора. Сбой без единицы (ошибка кода) лечит модуль
  целиком. Таблица `unit_health` (миграция `0003_unit_health.sql`), журналы `unit.strike`, `unit.quarantine`,
  `unit.quarantine.release` с меткой `by: time|operator`. Сроки единицы на карантине ждут (`deadline.postponed`) и
  догоняют ход после возврата; включение оператором снимает карантин и с единицы, и с модуля.
- **Догон после простоя.** `schedule.resume` (окно началось в простое и ещё идёт), `schedule.continue`
  (повседневное окно просто продолжается), `schedule.skipped` (окно прошло целиком мимо: `downtime.rotation`,
  `downtime.ended`, `module.off`). Окна, кончившиеся за простой, считаются отдельным расчётом по промежутку —
  в плане от настоящего мига их уже нет. Пропусков за долгий простой сотни, поэтому в журнал идёт **итог на единицу**
  (`unit`, `lane`, `count`, `firstKey`, `lastKey`), а сумма `count` сходится со счётчиком `scheduleMissed`.
  Перезапуск не дублирует `schedule.start`.
- **Счётчики и легенда.** `/api/health` отдаёт `unitsEnabled`, `unitsQuarantined`, `unitsQuarantineLocked`,
  `windowsActive`, `windowsSkipped`, `scheduleResumed`, `scheduleContinued`, `scheduleMissed`, `planHorizonMs`
  и двое часов: `calendarNow` (время сервера) и `worldNow` (ход мира). `/api/modules` отдаёт ещё и `quarantines`.

### Двое часов, и два дефекта, которые это вскрыло

События идут по календарю (время сервера, UTC+3), сроки игроков — по ходу мира, который стоит, пока процесс не работает.
В коде это было смешано: и окна, и сроки оператора, и карантин считались по ходу мира. На живой проверке это дало
цепочку дефектов — все починены:

- **Мир мог «застрять».** Сдвиг хода мира копился в базе (`worlds.clock_offset_ms`), а часы ещё и не шли назад.
  Испорченный или отодвинутый пульс (`real_at_ms` в прошлом) замораживал мир на всё это время: события стояли,
  сроки оператора не истекали — как раз то, что видел оператор. Теперь ход мира считается **из пульса**: в нём
  сходятся настоящие часы и мир на один миг. Запись из будущего или нулевая не принимается — журнал `pulse.suspect`,
  часы встают на настоящий миг, и мир не откатывается назад.
- **Срок возврата ставил «включено» вместо рождения.** Большое событие месяца рождено закрытым; после временного
  включения с истёкшим сроком оно становилось вечным. Теперь по сроку модуль возвращается в своё объявленное состояние.

### Проверено живьём на `world-1` (порядок проверки — как у оператора)

| Что | Как | Итог |
|---|---|---|
| Точечный запрет единицы | `POST { id: "season-autumn", unitId: "autumn", until: +5 с, reason }` | `disabled`/`operator`, единиц включено 3 → 2 |
| Срок возврата | ждём срок | `enabled`/`window`, единиц снова 3, строк в `unit_states` нет |
| Догон за трое суток | пульс отодвинут на 3 суток, рестарт (epoch 14) | `world.resume` `downtimeMs` 259206954, `schedule.continue` 1 (`wall-day`), `schedule.skipped` 6 с итогами по единицам, `scheduleMissed` 6 |
| События по календарю | `/api/schedule?days=1` при ходе мира на 6 суток позади | идут ровно сегодняшние окна: `autumn`, `hunt-week`, `wall-day` |
| Двое часов | `/api/health` | `calendarNow` − `worldNow` = простой мира, счётчики на месте |
| Карантины | `/api/modules` | 0 в силе, `month-window` вернулся к `module-closed` |

Карантин со сбоями живёт в тестах (`_kit.crash`, `_kit.slow`, `_kit.crash-module`, `_kit.arm-bad`): в живом мире
нет сбойного модуля, поэтому там проверяется путь оператора, а лечение — набором тестов на настоящем Postgres.

## Подготовка сезона и параллельный мир

**Примерка поры года.** Оператор включает пору вне календаря — и мир живёт по ней: вид идёт за порой (единица-спутник
`follows`), соседи в полосе `season` уступают (причина `yielded`, `yieldedTo` в панели), содержимое повседневных окон
берётся по примеряемой поре (зимой выпадают зимние дни). Примерка идёт с начала текущих суток и живёт своим сроком
`until`; без срока — одна пора. В журнале `schedule.rehearsal`, в `/api/health` — `seasonRehearsal`, в служебной полосе
клиента — «Примерка поры года». Снятие примерки возвращает календарь без следа: зимние окна примерки сняты, осенние возвращены.

**Параллельный мир** (`pnpm dev:parallel`, `docs/game/08-ops.md`): тот же код, свой `world_id` в той же базе, вход только
служебным логинам (`ACCESS_MODE=admins`, `ADMIN_LOGINS`). Игрок не заведёт аккаунт и не войдёт — ворота отвечают
`kernel.world.private`. Доработку проверяем там, в основной мир она уходит обычным рестартом.

**Выход из аккаунта.** Кнопка в панели «Модули»: сессия отзывается на сервере (`auth.logout` → `sessions` DELETE,
журнал `gate.logout`), токен убран из браузера, окно входа открывается снова — с регистрацией рядом, как в современных
играх. Клиент локально выходит сразу, чтобы окно откликалось.

## Что смотреть в предпросмотре

1. Вступление: восемь кадров, пропуск появляется со второго.
2. Вход: «Логин или почта», кнопка «Регистрация аккаунта» ведёт в отдельное окно — логин, почта, пароль, повтор, согласия.
3. Создание лорда: портрет, знак, цвет, тип, кнопка «Занять двор».
4. Двор: фраза про частокол; вкладка «Хроника» — там тексты слайдов.
5. Обновление страницы: двор на месте, слайды не повторяются.
6. Язык: переключение RU/EN переводит и хронику.
7. Служебное: `/api/health` — `epoch`, `dropped`, `deadlineLag`, `calendarNow`/`worldNow`, счётчики окон и карантина, состояния модулей; `/api/schedule?days=30` — что идёт и что впереди; `/api/modules` — переключатель модулей и единиц с ключом оператора (там же `quarantines`); `/api/kernel/rejections` — строки отказов.

## Как поднять у себя

```bash
cp .env.example .env      # вписать SESSION_SECRET, DATABASE_URL своего Postgres и ADMIN_TOKEN
pnpm install
pnpm dev:db               # или свой Postgres: миграции применятся при подъёме
pnpm --filter @tdl/server start
pnpm db:reset             # чистая база, если нужно
```

Проверки: `pnpm typecheck`, `pnpm test`, `pnpm check`, `pnpm build`.
Календарь: `pnpm check` (полосы за три года), `pnpm schedule --days 90` (распечатка плана).
